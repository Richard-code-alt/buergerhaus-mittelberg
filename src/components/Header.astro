---
import nav from '../data/seiten/navigation.json';

const navItems = [
  { href: '/anlaesse',   label: nav.nav_anlaesse },
  { href: '/hochzeit',   label: nav.nav_hochzeit },
  { href: '/raeume',     label: nav.nav_raeume },
  { href: '/catering',   label: nav.nav_catering },
  { href: '/galerie',    label: nav.nav_galerie },
  { href: '/partner',    label: nav.nav_partner },
  { href: '/anfahrt',    label: nav.nav_anfahrt },
];
const currentPath = Astro.url.pathname;
---

<header class="header" data-header>
  <div class="header__inner">
    <a href="/" class="header__brand" aria-label={nav.header_brand_label}>
      <img src="/logo.svg" alt={nav.header_logo_alt} width="280" height="80" />
    </a>

    <button class="header__toggle" data-menu-toggle aria-expanded="false" aria-controls="primary-nav" aria-label={nav.menu_open_label}>
      <span></span><span></span><span></span>
    </button>

    <nav id="primary-nav" class="header__nav" aria-label="Hauptnavigation">
      <ul class="header__list">
        {navItems.map((item) => (
          <li>
            <a
              href={item.href}
              class={currentPath === item.href || (item.href !== '/' && currentPath.startsWith(item.href)) ? 'is-current' : ''}
              aria-current={currentPath === item.href ? 'page' : undefined}
            >{item.label}</a>
          </li>
        ))}
      </ul>
      <a href="/anfragen" class="btn btn--secondary header__cta">{nav.nav_anfrage_button}</a>
    </nav>
  </div>
</header>

<style>
  .header {
    position: sticky; top: 0; z-index: 50;
    background: var(--c-cream-warm);
    border-bottom: 1px solid var(--c-line);
    padding: var(--s-4) 0;
    box-shadow: 0 1px 0 rgba(31, 31, 31, 0.04);
  }
  .header__inner {
    display: flex; align-items: center; justify-content: space-between;
    gap: var(--s-4);
    padding: 0 var(--s-4);
    margin: 0 auto;
    max-width: var(--w-wide);
  }
  @media (min-width: 720px) {
    .header { padding: var(--s-5) 0; }
    .header__inner { padding: 0 var(--s-6); gap: var(--s-5); }
  }

  .header__brand img {
    height: 120px; width: auto; display: block;
  }
  @media (min-width: 720px) {
    .header__brand img { height: 144px; }
  }
  @media (min-width: 1100px) {
    .header__brand img { height: 168px; }
  }

  .header__list {
    display: none;
  }
  .header__cta {
    display: none;
  }

  /* Mobile-Toggle */
  .header__toggle {
    display: flex;
    width: 32px; height: 32px;
    flex-direction: column; justify-content: center; gap: 5px;
    padding: 0;
  }
  .header__toggle span {
    display: block; width: 100%; height: 2px; background: var(--c-anthracite);
    transition: transform var(--transition), opacity var(--transition);
  }
  .header__toggle[aria-expanded="true"] span:nth-child(1) { transform: translateY(7px) rotate(45deg); }
  .header__toggle[aria-expanded="true"] span:nth-child(2) { opacity: 0; }
  .header__toggle[aria-expanded="true"] span:nth-child(3) { transform: translateY(-7px) rotate(-45deg); }

  .header__nav {
    position: fixed; top: 152px; left: 0; right: 0; bottom: 0;
    background: var(--c-cream-warm);
    padding: var(--s-7) var(--s-5);
    flex-direction: column; align-items: stretch; gap: var(--s-5);
    transform: translateX(100%); opacity: 0; pointer-events: none;
    transition: transform var(--transition), opacity var(--transition);
    z-index: 49;
    overflow-y: auto;
  }
  .header__nav[data-open="true"] {
    display: flex;
    transform: translateX(0); opacity: 1; pointer-events: auto;
  }
  .header__nav .header__list {
    display: flex; flex-direction: column; gap: var(--s-4);
  }
  .header__nav .header__list a {
    font-family: var(--f-body); font-weight: 600; text-transform: uppercase; letter-spacing: 0.1em;
    font-size: var(--t-xl);
    color: var(--c-anthracite);
    padding: var(--s-3) 0;
    display: block;
    border-bottom: 1px solid var(--c-line-light);
  }
  .header__nav .header__list a:hover, .header__nav .header__list a.is-current {
    color: var(--c-blue);
  }
  .header__nav .header__cta {
    display: inline-flex; align-self: flex-start;
    margin-top: var(--s-4);
    padding: var(--s-4) var(--s-6);
    justify-content: center;
    font-size: var(--t-md);
  }

  @media (min-width: 900px) {
    .header__toggle { display: none; }
    .header__nav {
      position: static; transform: none; opacity: 1; pointer-events: auto;
      flex-direction: row; align-items: center; gap: var(--s-5);
      padding: 0; background: none; overflow: visible;
    }
    .header__list {
      display: flex !important; flex-direction: row !important;
      gap: var(--s-5); list-style: none;
    }
    .header__nav .header__list a {
      font-size: var(--t-sm);
      padding: var(--s-3) 0 var(--s-2);
      border-bottom: 0;
      letter-spacing: 0.1em;
      position: relative;
    }
    .header__nav .header__list a::after {
      content: ''; position: absolute; bottom: 0; left: 0; right: 0; height: 2px;
      background: transparent;
      transition: background-color var(--transition);
    }
    .header__nav .header__list a:hover::after,
    .header__nav .header__list a.is-current::after {
      background: var(--c-mokka);
    }
    .header__cta {
      display: inline-flex !important;
      padding: 0.6rem 1.4rem !important;
      font-size: var(--t-sm) !important;
      margin-top: 0 !important;
      margin-left: var(--s-3) !important;
    }
  }
</style>

<script>
  const toggle = document.querySelector('[data-menu-toggle]');
  const nav = document.getElementById('primary-nav');
  if (toggle && nav) {
    toggle.addEventListener('click', () => {
      const open = toggle.getAttribute('aria-expanded') === 'true';
      toggle.setAttribute('aria-expanded', String(!open));
      nav.setAttribute('data-open', String(!open));
      document.body.style.overflow = !open ? 'hidden' : '';
    });

    // Close on link click
    nav.querySelectorAll('a').forEach((a) => {
      a.addEventListener('click', () => {
        toggle.setAttribute('aria-expanded', 'false');
        nav.setAttribute('data-open', 'false');
        document.body.style.overflow = '';
      });
    });
  }
</script>
